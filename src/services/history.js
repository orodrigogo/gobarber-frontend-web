// Vamos usar o history para chamar navegacao de tela de todos os lugares da aplicacao, inclussive de dentro do reduxyr.
import { createBrowserHistory } from "history";

const history = createBrowserHistory();

export default history;
