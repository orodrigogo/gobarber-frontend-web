import { all } from "redux-saga/effects";

/*
  Redux Saga é uma biblioteca que visa criar efeitos
  colaterais. Ela ficará escutando as Actions que pedirmos para disparar Sagas.
*/
export default all([]);
